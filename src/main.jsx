import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App.jsx'
import './index.css' // No importa si está vacío, App.jsx ya tiene sus estilos

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)
3. Haz el `git add .`, `git commit -m "Solución pantalla blanca"` y `git push`.

